import theme from '../theme/Theme';

const useListStyle = {
    paper: {
        padding: '1%',
        display: 'flex',
        alignItems: 'center',
        width: '80%',
        background: theme.palette.secondary.main,
    },
};

export default useListStyle;